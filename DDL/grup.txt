CREATE TABLE "GRUP"(
    "ID_GRUP" NUMBER(4),
    "NR_MEM" NUMBER(4),
    "NUME_SOFER" VARCHAR2(50) NOT NULL,
    "ID_PV" NUMBER(4),
    "ID_MASINA" NUMBER(4),
    PRIMARY KEY("ID_GRUP") USING INDEX ENABLE
);

Constraint "ID_PV" foreign key:
ALTER TABLE "GRUP" ADD CONSTRAINT "IDPV_CON_GRUP" FOREIGN KEY ("ID_PV")
	REFERENCES "PUNCT VAMAL" ("ID_PV") ON DELETE CASCADE ENABLE;


Constraint "ID_MASINA" foreign key:
ALTER TABLE "GRUP" ADD CONSTRAINT "IDMASINA_CON_GRUP" FOREIGN KEY ("ID_MASINA")
	  REFERENCES "MASINA" ("ID_MASINA") ON DELETE CASCADE ENABLE;
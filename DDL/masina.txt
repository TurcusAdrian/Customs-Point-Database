CREATE TABLE "MASINA"(
    "ID_MASINA" NUMBER(4),
    "NR_INMATR" VARCHAR2(20) NOT NULL,
    "MODEL" VARCHAR2(70),
    "AN_FABR" NUMBER(4),
    PRIMARY KEY("ID_MASINA") USING INDEX ENABLE
);


ALTER TABLE MASINA
ADD CONSTRAINT chk_an_fabr_range
CHECK (AN_FABR BETWEEN 1885 AND 2024);



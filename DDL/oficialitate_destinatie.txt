CREATE TABLE "OFICIALITATE_DESTINATIE"(
    "NUME" VARCHAR2(50),
    "ID_DEST" NUMBER(4)
);

ALTER TABLE "OFICIALITATE_DESTINATIE"
MODIFY "NUME" VARCHAR2(50) NOT NULL;

Constraint NUME foreign key:
ALTER TABLE "OFICIALITATE_DESTINATIE" ADD CONSTRAINT "NUME_CON_OFICIALITATE_DESTINATIE" FOREIGN KEY ("NUME")
	  REFERENCES "OFICIALITATE" ("NUME") ON DELETE CASCADE ENABLE;


Constraint ID_DEST foreign key:
ALTER TABLE "OFICIALITATE_DESTINATIE" ADD CONSTRAINT "IDDEST_CON_OFICIALITATE_DESTINATIE" FOREIGN KEY ("ID_DEST")
	  REFERENCES "DESTINATIE" ("ID_DEST") ON DELETE CASCADE ENABLE;